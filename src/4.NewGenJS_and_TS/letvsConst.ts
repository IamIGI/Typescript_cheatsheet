const userName3 = 'Max';
// userName = 'Maximilian';    //can not assing again to the same variable

let age = 30;
age = 20;

function add5(a: number, b: number) {
    let result;
    result = a + b;
    return result;
}

console.log(add5(4, 2));
// console.log(result);    //can not find result variable
